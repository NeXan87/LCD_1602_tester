#pragma once

#include "core/screen-id.h"

// Инициализация экрана тестера энкодера (подменю)
void initScreenEncoderTest();

// Обновление экрана тестера энкодера
// @return ScreenId следующего экрана
ScreenId updateScreenEncoderTest();
